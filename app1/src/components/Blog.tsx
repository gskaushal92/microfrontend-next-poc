import { FC } from "react";

interface BlogProps {}

const Blog: FC<BlogProps> = ({}) => {
  return (
    <div>
      Blog
      <div>another update for app1</div>
    </div>
  );
};

export default Blog;
